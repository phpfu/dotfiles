# You are encouraged to put your secret environment variables in ~/.secrets
# as this will automatically be source for you (e.g. GITHUB_API_TOKEN, etc)

if [ -e ~/.secrets ]; then
  source ~/.secrets
fi
